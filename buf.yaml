# Documentation for the buf.yaml file https://buf.build/docs/configuration/v2/buf-yaml/
version: v2

modules:
  - path: proto

lint:
  use:
    - STANDARD
  except:
    - PACKAGE_VERSION_SUFFIX
    - SERVICE_PASCAL_CASE
    - SERVICE_SUFFIX
